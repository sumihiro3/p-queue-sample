// p-queue-sampleのサンプルコード
console.log('TypeScriptプロジェクトが正常に初期化されました！');

// 簡単な関数の例
function greet(name: string): string {
  return `こんにちは、${name}さん！`;
}

// 関数の実行
const message = greet('TypeScript');
console.log(message);